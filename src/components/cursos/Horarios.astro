---
interface Props {
  f_inicio?: string;
  f_termino?: string;
  formato: string;
}
const { f_inicio, f_termino, formato } = Astro.props;
---

<div class="bg-base-100 shadow-md rounded-md card">
  <div class="flex gap-4 max-w-full card-body prose">
    <h3 class="mb-1 font-bold text-sky-600 text-4xl">Fechas y Horarios</h3>
    <div set:html={formato} />
    <div class="flex gap-2 mt-4">
      {
        f_inicio && (
          <p class="bg-sky-600 p-2 rounded-md text-white uppercase">
            Fecha Inicio: <span class="font-bold">{f_inicio}</span>
          </p>
        )
      }
      {
        f_termino && (
          <p class="bg-sky-600 p-2 rounded-md text-white uppercase">
            Fecha Termino: <span class="font-bold">{f_termino}</span>
          </p>
        )
      }
    </div>
  </div>
</div>
